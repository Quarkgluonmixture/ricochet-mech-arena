# GOTCHAS — 现役陷阱（编号只增、永不重排；正文随认识改写）

动手前按你正在哪一步挑着读。每条：症状 → 机制 → ⇒ 规则，带 `闸:` 一行说明机器管不管得住。

| 你要动 / 你正在哪一步 | 先读 |
|---|---|
| 改人机打分、射击解、几何判断 | **3 5 6 7** |
| 改模式切换、阵容、任何拿着 `world`/`player` 引用的代码 | **16** |
| 改灯光、材质、后处理、FX、任何随时间变化的东西 | **2 10 11 13 18** |
| 跑无头验证、截图、拿性能数 | **1 12 14 15 17** |
| 元规律 | **A B** |

## 元规律 / 族

- **A · 两套几何各说各话**：视线用零宽射线、射击用炮弹半径膨胀后的墙；炮口从中心前 1.13 m 起算、墙内射线看不见墙。
  凡是「一处判断能、另一处判断不能」的僵局，先查两边用的是不是同一套几何（坑 4、6 同族）。
- **B · 无头环境不是真机**：swiftshader 的帧时间、pointer lock、偶发白屏都是环境产物；它能证「逻辑对」，
  不能证「手感对 / 快 / 看得清」（坑 12、14、15 同族）。

## 改人机打分、射击解、几何判断之前

3. **症状**：人机永远不开火。**机制**：反弹解每次取最短路径，目标一动解就在几堵墙之间跳，炮塔（有限转速）永远追不到位。
   ⇒ 解的选择必须带「离当前炮塔朝向近」的代价（`findFireSolution` 的 `preferYaw`）。
   闸: 无 — 频率退化不报错，测试只断言「有解」不断言「开得出火」。
5. **症状**：炮塔和腿耦合后开火频率掉一个量级（5 rad/s 时 15 s 只有 1–2 发）。**机制**：腿一转炮塔跟着偏，回正慢于走位变化。
   ⇒ 调侵略性先动 `torsoTurnRateAI`；⛔ 别放宽 `aimTolerance`，那是精度不是频率。
   闸: 无 — 同 3，靠对局计数（LOG #measure）而非断言。
6. **症状**：两台人机在 13 m 外面对面永久站定，各开一枪后再不动。**机制**：打分用零宽视线判「能打」，射击解用 `shellWalls`
   判「无解」，两边同时成立就没人挪。⇒ 给人机打分的几何判断一律用 `shellWalls`；无解超过 0.8 s 进入找角度状态。（族 A）
   闸: 无 — 是行为僵局不是断言可抓的错误；观战 90 s 开火计数是唯一检测手段。
7. **症状**：站着不动的人机原地打转。**机制**：站立时腿转向炮塔，若炮塔又被腿「带着转」，两者互相追。
   ⇒ 炮塔跟腿只在**移动时**生效（`aiThink` 里 `len(vel) > 0.5`）。
   闸: 无 — 视觉症状，测试不看动画。

## 改模式切换、阵容之前

16. **症状**：换阵容或从菜单进入对局后，某处代码还在读旧的机甲/世界（分数不动、位置不更新、探针拿到死对象）。
    **机制**：`buildRoster()` 用新的 `World` 和新的 mech 对象整体替换 `world` / `player` / `slots`，旧引用不会报错、只是过期。
    ⇒ 一律通过模块级 `let`（`world`、`player`）或 `slots[id]` 现取，⛔ 别把 mech/world 存进闭包或类字段；
    `window.rma` 因此用 getter 而不是属性。进 Play 走 `ensureRoster('pve')`，进观战/attract 走 `ensureRoster('eve')`。
    闸: 无 — 过期引用不抛错；靠无头脚本里 `probe().roster` 与 `alive` 的对照发现。

## 改灯光、材质、后处理、FX 之前

2. **症状**：场景一片黑或一片白。**机制**：three r155+ 光强是物理量级，且 bloom 阈值与被照亮表面的线性亮度是同一把尺；
   太阳/天光一调，墙顶就整片过阈值发光。⇒ 只让 HDR 自发光体（倍率 1.5–2.0）过阈值；改光先截图，数值以 `src/render/scene.ts` 为准。
   闸: 无 — 亮度对错只能看图。
10. **症状**：贴图加载中或缺文件时物体全黑。**机制**：未加载完成的 `map` 渲染成黑而不是「无贴图」。
    ⇒ `map` 一律在 load 回调里赋值（`getTexture`），缺文件保留平色；游戏必须在删掉 `public/textures/` 后照常能玩。
    闸: 无 — 需要断网/删文件的手动检查。
11. **症状**：每开一枪、每颗炮弹消失都顿一下。**机制**：lit shader 按场景灯数编译，灯数一变全部重编译。
    ⇒ ⛔ 别按事件增删 PointLight，用固定池 + `intensity 0` 表示空闲（`ShellViews` / `Fx`），只在改画质时变一次。
    闸: 无 — 卡顿不报错；真机「显示帧时间」是唯一读数。
13. **症状**：导演镜头画面右下角一个大灰块。**机制**：驾驶舱枪模型挂在相机下面。
    ⇒ 任何用导演相机的模式（观战、attract、玩家死后）都要 `setViewmodelVisible(false)`；`main.ts` 每帧按 `directorCam` 设。
    闸: 无 — 视觉症状。
18. **症状**：击杀慢放时命中爆闪变成一个 1.8 s 的半透明巨型圆顶罩住残骸；或反过来，HUD 计时 / 相机在慢放里被拖慢。
    **机制**：主循环自 2026-09-13 起有两套时钟——`dt`（屏幕时间）和 `sdt = dt × 时间尺度`（世界时间），两个都是 number，
    用错一个不报错。⇒ 新加任何随时间变化的东西先选时钟：**世界时间** = 机甲动画、碎片、尘埃、弹尾采样、模拟步进；
    **屏幕时间** = 相机平滑、HUD 计时、爆闪 / 光环（光是瞬间的）、UI 音。`fx.update(dt, sdt)` 两个都收。
    闸: 无 — 类型上分不开。

## 跑无头验证、截图、拿性能数之前

1. **症状**：脚本玩家在出生点朝墙连发，自己被反弹打死，数据全是乌龙。**机制**：出生点正对 4 m 外的墙，selfArmTime 后反弹弹可打死自己（设计内）。
   ⇒ 测对射把双方摆到 `MAP_A` 第 5 行（z = -4，x ∈ [-12, 12]）开阔走廊；或先把 `maxShells` 设 0 再摆位。
   闸: 无 — 测试脚本纪律。
12. **症状**：无头里 200 ms/帧。**机制**：swiftshader 软渲染。⇒ 性能结论只来自真机「显示帧时间」读数。（族 B）
    闸: 无 — 环境限制。
14. **症状**：无头里 Play 之后 `locked` 仍是 false、Resume 链走不通。**机制**：headless Chromium 的 `requestPointerLock` 必失败。
    ⇒ 这条链只能真机验；脚本里用 `rma.attract(false); hud.setOverlay(false); world.resetMatch()` 模拟 Play 的副作用。（族 B）
    闸: 无 — 环境限制。
15. **症状**：截图整页发白、HUD 半透明、左上角破图标。**机制**：合成器偶发。⇒ 同一状态再截一次；⛔ 别按白图改渲染。（族 B）
    闸: 无 — 偶发且不可复现。
17. **症状**：class 明明已经加上（`className` 有 `on`），截图里却没有黑边 / 渐变，像是 CSS 没生效。**机制**：无头帧约 0.4 s
    一帧，CSS transition 由合成器在墙钟上推进，截图落在过渡的起点（实测 0.3 s 时 computed opacity 仍为 0，0.9 s 才到 1）。
    ⇒ 判 UI 状态先 dump `className` + `getComputedStyle`，或在状态进入 ≥ 1 s 后再截；⛔ 别按早截的图改 CSS。（族 B）
    闸: 无 — 环境产物。

## 墓碑（已被闸完全覆盖或已根治，正文见 LOG 对应日期）

- 4 · 炮口在墙内导致穿墙（2026-09-13）—— 闸: `tests/world.test.ts`（贴墙点射、离墙 1 m 斜射两条）+ `advanceShell` 墙内销毁兜底。
- 8 · 快进模拟一帧产出上百个 FX 灯把渲染拖死（2026-09-13）—— 闸: `src/render/fx.ts` 固定 4 盏灯池，与坑 11 同解。
- 9 · 墙顶灯带是整块薄板导致「墙顶太亮」（2026-09-13）—— 已根治为四边细框，见 `src/render/scene.ts`。

## 准入判据

进这里的条目必须同时满足：**未来还会遇到** ∧ **发生时不报错**。会报错的、现有测试抓得到的、只发生过一次的，
分流到 `LOG.md` 的 `#incident` 或一条测试。被闸完全覆盖的条目退休成墓碑，正文留在 LOG。
