# New-dependency cooldown: never install a release younger than 7 days (supply-chain poisoning is
# usually pulled within hours-to-days). Needs npm >= 11.10; older npm silently ignores this line.
min-release-age=7
